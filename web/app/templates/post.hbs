<div class="container" >
  <div class="card">
    <div class="card-body">
        <LinkTo @route="posts">
          <i class="bi bi-arrow-left-short"></i> Voltar
        </LinkTo>
        <h1>{{@model.title}}</h1>
        <p>{{format-date @model.updated_at "dd 'de' MMMM yyyy"}}</p>
        <p>{{safe-render @model.content}}</p>

    </div>
  </div>
</div>

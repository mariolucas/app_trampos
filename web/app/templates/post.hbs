<div class="container" >
  <div class="card">
    <div class="card-body">
        <h1>{{@model.title}}</h1>
        <p>{{@model.content}}</p>
        <LinkTo @route="posts" class="btn btn-primary">
          Voltar
        </LinkTo>
    </div>
  </div>
</div>
